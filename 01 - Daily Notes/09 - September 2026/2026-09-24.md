---
status: active
project: tsa
type: log
created: 2026-09-24
---

# Thursday, September 24, 2026

## Index

- **Eleven commits recovered off a stranded branch, and the queue item that would have destroyed them corrected** — `Active Priorities` told Aly every session branch was contained in `main` and safe to delete. It was not true: `claude/foundation-work-summary-o9lifj` held the whole 22 September day, three sessions of the 23rd, and three notes that did not exist on the trunk. Merged, verified, and the item rewritten with the real count.
- **Structural audit of the vault, creation to now — and the rulebook was describing a state that had been reverted** — `VAULT-INDEX` claimed the four asset indexes had been renamed off `README.md`. They are all `README.md` on disk; the 20 September client-folder move undid the 19 September rename and nothing noticed for four days. Also found the only folder index missing its `Up:` line. Both fixed.

---

## Session 1 — 12:15 AM: The stranded branch, and a queue item that was lying

### What Got Done
- **Ran the two containment commands the queue itself specifies**, rather than trusting its word: `git ls-remote --heads origin`, then `git merge-base --is-ancestor <branch> origin/main` for each. **The remote carries sixteen session branches, not the thirteen the note claimed**, and two of them were not contained in `main`.
- **Found `claude/foundation-work-summary-o9lifj` holding eleven commits that had never reached the trunk.** `claude/team-deliverables-payment-ynwuvi` turned out to be the same tree nine commits back — one stranded branch, not two. What was on it and nowhere else: the entire `2026-09-22.md` daily note, three sessions of the 23rd (the Foundation recap, Local SEO as deliverable 8, and the Facebook and Instagram bios), plus `Mahmoud Kit Brief`, `Mahmoud Engagement` and `Local SEO — Alexandria`.
- **Merged it into the trunk line.** Two conflicts, both in append-only logs, so both sides were correct and both survived: `2026-09-23.md` kept its three stranded sessions and took graphify as **Session 4**, and `Daily Notes.md` took the fuller branch list with graphify folded into the 23 September line. Nothing was overwritten in either file.
- **Verified the merge added zero findings.** `structure.py` reports clean; `check.py` returns the same two pre-existing retired-claim findings it returned before the merge — the POLEKA line in `book.py:261` and page 15 of the shipped book.
- **Rewrote the branch-deletion item in `Active Priorities` §8** with the verified count, the branch that must not be deleted before the merge lands, and an instruction to re-run the two commands rather than trust the line.
- **Added a new §8 item: rule 17 has broken four times**, and what is missing is not the rule but a check that catches a session which ignored it.

### What's Still In Progress
- **The merge is committed and pushed to `claude/vault-organization-irn5ti`, and it is NOT on `main`.** Rule 2 puts a push to the trunk behind Aly's go-ahead and rule 14b bars a commit on a red check, so the intent was to hold it uncommitted — but this container is ephemeral and the conflict resolutions existed nowhere else, so it was committed to the session branch to stop the recovery being lost a second time. **The irony is the point: that makes seventeen branches and this one is now the stranded tree.** It has to reach `main` in Aly's next session or the whole exercise repeats. **Until it lands, none of the seventeen branches may be deleted.**
- **`vault-check` is still red on the two POLEKA findings**, exactly as it was before this session. They need the `book.py` edit and a re-render, both queued and both waiting on Aly.

### Decisions Made
- **The daily-note conflicts resolve by keeping both sides, never by choosing one.** A daily note is an append-only log, so two sessions writing the same day are both true. Frontmatter took `project: tsa` per the vault's own default for daily notes.
- **No source-code or tool-config edit was made.** The three §8 items that need one — `structure.py`'s asset-folder list, `owners.tsv`'s bare-number patterns, and `book.py` — were left alone and stay queued.

### Notes Touched
- `Active Priorities` — branch items rewritten, rule-17 item added, date line moved to 24 September
- `2026-09-23.md` — the three stranded sessions restored, graphify renumbered to Session 4
- `Daily Notes.md` — 2026-09-22 and the fuller 21st and 23rd entries restored
- `2026-09-22.md`, `Mahmoud Kit Brief`, `Mahmoud Engagement`, `Local SEO — Alexandria` — recovered, previously absent from the trunk

### Profile Updates
- None.

## Session 2 — 12:29 AM: The structural audit, creation to now

### What Got Done
- **Traced how the vault was actually built.** It was not grown, it was **designed whole and then populated**: the first commit at 00:18 on 18 September already carried all five numbered folders, every folder index, `CLAUDE.md`, `VAULT-INDEX.md` and the whole Marketing playbook — 32 files. Everything since is content and correction, not skeleton.
- **Dated every folder's birth.** 18 Sep: the five numbered folders plus `TSA Brand/`. **20 Sep is the restructure day** — 54 commits, and it created `Clients/`, `Clients/Alex Foods/`, `Alex Foods Brands/`, `Jobs/` and `vault-check/` in one day. 21 Sep: `el-ghaly-screens/`. Nothing structural since.
- **Measured the end state:** 79 notes, 122 assets, 13 generators, 12 PDFs, 14 folders. **~175,000 words of reference and ~68,000 words of daily log** across 124 commits in six days.
- **Audited the structure against the rules, by command rather than by reading the map.** Clean: every note is named in its folder index (zero misses), frontmatter is valid on all 78 notes, every folder either has its index note or correctly has none, and `structure.py` reports clean.
- **Found and fixed the rulebook describing a reverted state.** `VAULT-INDEX` said the four asset indexes were renamed to `design-system.md`, `packshots.md`, `logos-transparent.md`, `logo-vector.md`. **All four are `README.md` on disk.** The rename did happen on 19 September; the 20 September move of Alex Foods into `Clients/` rebuilt the folders with `README.md` and silently reverted it, and the paragraph stood uncorrected for four days while a carve-out forty lines above it asserted the opposite. Rewritten as marked history that names the reversal.
- **Fixed the one folder index missing its `Up:` line** — `Inbox.md`, the only one of thirteen without it.

### What's Still In Progress
- **The hub is still net outbound: 23 inbound against 36 outbound wikilinks.** `VAULT-INDEX`'s own stated aim is that *"a hub is inbound-heavy."* It is no longer a pure source, which was the original defect, but it has not reached its own goal. No numeric target is written anywhere, so this is an observation rather than a failure.
- **`el-ghaly-screens/` still carries a note-style index** instead of a `README.md`, the workaround for the missing entry in `structure.py`'s asset-folder list. Already queued in `Active Priorities` §8 and still needs Aly's go-ahead on the one-word source change.
- **The Vault Structure map omits three sub-sub folders** — `design-system/fonts/`, `design-system/tsafonts/` and `packshots/single/`. They are described in the carve-out prose but not drawn. Left alone: the map is deliberately shallow and redrawing it is not a session's call.

### Decisions Made
- **The disk wins over the map, always.** The audit was run as commands against the filesystem and git, not as a read of `VAULT-INDEX`. That is the only reason the reverted rename surfaced — reading the document would have confirmed it.

### Notes Touched
- `VAULT-INDEX` — the asset-index paragraph rewritten as marked history
- `Inbox` — `Up:` line added
- `2026-09-24` — this session

### Profile Updates
- None.
