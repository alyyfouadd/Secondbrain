---
status: active
project: meta
type: index
---
# Archive

Finished projects and notes that are done but worth keeping. Nothing gets deleted — it gets archived, with `status: archived` in its frontmatter and the same filename it had before.

Archiving is never something the AI decides on its own. I say a thing is done, the AI confirms, and only then does it move.

## Notes in this folder
*(Empty. This index exists from day one so the folder survives a git sync — git doesn't track empty folders.)*
