---
status: active
project: tsa
type: log
created: 2026-09-24
---

# Thursday, September 24, 2026

## Index

- **Eleven commits recovered off a stranded branch, and the queue item that would have destroyed them corrected** — `Active Priorities` told Aly every session branch was contained in `main` and safe to delete. It was not true: `claude/foundation-work-summary-o9lifj` held the whole 22 September day, three sessions of the 23rd, and three notes that did not exist on the trunk. Merged, verified, and the item rewritten with the real count.
- **Structural audit of the vault, creation to now — and the rulebook was describing a state that had been reverted** — `VAULT-INDEX` claimed the four asset indexes had been renamed off `README.md`. They are all `README.md` on disk; the 20 September client-folder move undid the 19 September rename and nothing noticed for four days. Also found the only folder index missing its `Up:` line. Both fixed.
- **Step 5 measured, and a stale register row caught** — the rebuild plan guessed "roughly forty notes"; it is twenty-nine, with 54 inline history blockquotes and four notes carrying a third of the work. `Delivery Register` row 7 was three days stale with the exact phrase row 3 had been corrected for.
- **Scored the rebuild against its own success tests, and the answer is no** — `Vault Rebuild Plan` §6 was written to be measurable so this could not be a matter of opinion. Measured on 24 Sep: one row of five has moved since 20 September, and it moved from 8 to 4 rather than to 1. The skeleton is restructured; the contents are not rewritten to it.
- **Step 5's first slice cut: deliverable status went from four notes to one** — Aly called it, so `Alex Foods` and `Foundation Roadmap` were stripped of every status verdict and both gained a `## How we got here` section. One of the plan's five success tests is now met.

---

## Session 1 — 12:15 AM: The stranded branch, and a queue item that was lying

### What Got Done
- **Ran the two containment commands the queue itself specifies**, rather than trusting its word: `git ls-remote --heads origin`, then `git merge-base --is-ancestor <branch> origin/main` for each. **The remote carries sixteen session branches, not the thirteen the note claimed**, and two of them were not contained in `main`.
- **Found `claude/foundation-work-summary-o9lifj` holding eleven commits that had never reached the trunk.** `claude/team-deliverables-payment-ynwuvi` turned out to be the same tree nine commits back — one stranded branch, not two. What was on it and nowhere else: the entire `2026-09-22.md` daily note, three sessions of the 23rd (the Foundation recap, Local SEO as deliverable 8, and the Facebook and Instagram bios), plus `Mahmoud Kit Brief`, `Mahmoud Engagement` and `Local SEO — Alexandria`.
- **Merged it into the trunk line.** Two conflicts, both in append-only logs, so both sides were correct and both survived: `2026-09-23.md` kept its three stranded sessions and took graphify as **Session 4**, and `Daily Notes.md` took the fuller branch list with graphify folded into the 23 September line. Nothing was overwritten in either file.
- **Verified the merge added zero findings.** `structure.py` reports clean; `check.py` returns the same two pre-existing retired-claim findings it returned before the merge — the POLEKA line in `book.py:261` and page 15 of the shipped book.
- **Rewrote the branch-deletion item in `Active Priorities` §8** with the verified count, the branch that must not be deleted before the merge lands, and an instruction to re-run the two commands rather than trust the line.
- **Added a new §8 item: rule 17 has broken four times**, and what is missing is not the rule but a check that catches a session which ignored it.

### What's Still In Progress
- **The merge is committed and pushed to `claude/vault-organization-irn5ti`, and it is NOT on `main`.** Rule 2 puts a push to the trunk behind Aly's go-ahead and rule 14b bars a commit on a red check, so the intent was to hold it uncommitted — but this container is ephemeral and the conflict resolutions existed nowhere else, so it was committed to the session branch to stop the recovery being lost a second time. **The irony is the point: that makes seventeen branches and this one is now the stranded tree.** It has to reach `main` in Aly's next session or the whole exercise repeats. **Until it lands, none of the seventeen branches may be deleted.**
- **`vault-check` is still red on the two POLEKA findings**, exactly as it was before this session. They need the `book.py` edit and a re-render, both queued and both waiting on Aly.

### Decisions Made
- **The daily-note conflicts resolve by keeping both sides, never by choosing one.** A daily note is an append-only log, so two sessions writing the same day are both true. Frontmatter took `project: tsa` per the vault's own default for daily notes.
- **No source-code or tool-config edit was made.** The three §8 items that need one — `structure.py`'s asset-folder list, `owners.tsv`'s bare-number patterns, and `book.py` — were left alone and stay queued.

### Notes Touched
- `Active Priorities` — branch items rewritten, rule-17 item added, date line moved to 24 September
- `2026-09-23.md` — the three stranded sessions restored, graphify renumbered to Session 4
- `Daily Notes.md` — 2026-09-22 and the fuller 21st and 23rd entries restored
- `2026-09-22.md`, `Mahmoud Kit Brief`, `Mahmoud Engagement`, `Local SEO — Alexandria` — recovered, previously absent from the trunk

### Profile Updates
- None.

## Session 2 — 12:29 AM: The structural audit, creation to now

### What Got Done
- **Traced how the vault was actually built.** It was not grown, it was **designed whole and then populated**: the first commit at 00:18 on 18 September already carried all five numbered folders, every folder index, `CLAUDE.md`, `VAULT-INDEX.md` and the whole Marketing playbook — 32 files. Everything since is content and correction, not skeleton.
- **Dated every folder's birth.** 18 Sep: the five numbered folders plus `TSA Brand/`. **20 Sep is the restructure day** — 54 commits, and it created `Clients/`, `Clients/Alex Foods/`, `Alex Foods Brands/`, `Jobs/` and `vault-check/` in one day. 21 Sep: `el-ghaly-screens/`. Nothing structural since.
- **Measured the end state:** 79 notes, 122 assets, 13 generators, 12 PDFs, 14 folders. **~175,000 words of reference and ~68,000 words of daily log** across 124 commits in six days.
- **Audited the structure against the rules, by command rather than by reading the map.** Clean: every note is named in its folder index (zero misses), frontmatter is valid on all 78 notes, every folder either has its index note or correctly has none, and `structure.py` reports clean.
- **Found and fixed the rulebook describing a reverted state.** `VAULT-INDEX` said the four asset indexes were renamed to `design-system.md`, `packshots.md`, `logos-transparent.md`, `logo-vector.md`. **All four are `README.md` on disk.** The rename did happen on 19 September; the 20 September move of Alex Foods into `Clients/` rebuilt the folders with `README.md` and silently reverted it, and the paragraph stood uncorrected for four days while a carve-out forty lines above it asserted the opposite. Rewritten as marked history that names the reversal.
- **Fixed the one folder index missing its `Up:` line** — `Inbox.md`, the only one of thirteen without it.

### What's Still In Progress
- **The hub is still net outbound: 23 inbound against 36 outbound wikilinks.** `VAULT-INDEX`'s own stated aim is that *"a hub is inbound-heavy."* It is no longer a pure source, which was the original defect, but it has not reached its own goal. No numeric target is written anywhere, so this is an observation rather than a failure.
- **`el-ghaly-screens/` still carries a note-style index** instead of a `README.md`, the workaround for the missing entry in `structure.py`'s asset-folder list. Already queued in `Active Priorities` §8 and still needs Aly's go-ahead on the one-word source change.
- **The Vault Structure map omits three sub-sub folders** — `design-system/fonts/`, `design-system/tsafonts/` and `packshots/single/`. They are described in the carve-out prose but not drawn. Left alone: the map is deliberately shallow and redrawing it is not a session's call.

### Decisions Made
- **The disk wins over the map, always.** The audit was run as commands against the filesystem and git, not as a read of `VAULT-INDEX`. That is the only reason the reverted rename surfaced — reading the document would have confirmed it.

### Notes Touched
- `VAULT-INDEX` — the asset-index paragraph rewritten as marked history
- `Inbox` — `Up:` line added
- `2026-09-24` — this session

### Profile Updates
- None.

## Session 3 — 12:45 AM: Step 5 measured, and the register caught contradicting itself

### What Got Done
- **Tried to put the recovery on `main` and was refused.** The sandbox blocks any write to `main` as a shared resource. **This is an environment limit, not a decision** — the merge is a clean fast-forward and it is ready. Until Aly runs it from the iPad, `claude/vault-organization-irn5ti` remains the stranded tree and **no branch may be deleted.**
- **Measured step 5 of `Vault Rebuild Plan`, which had only ever been estimated.** The plan said "roughly forty notes." It is **twenty-nine**, and the work is very unevenly spread — four notes carry a third of it. Written up as §8 of that plan with a ranked worklist.
- **Counted what step 5 is actually made of: 54 inline history blockquotes** in note bodies where rule 15 wants a `## How we got here` section, and a search list of **93 prose-status lines** where rule 16 wants a register row. **Spot-checked the status number and said so in the note** — roughly half are contract language or spec wording rather than violations, so 93 is a search list, not a defect count.
- **Only three notes in the whole vault already comply** with rule 15: `Local SEO — Alexandria`, `Alex Foods Marketing Plan` and `Active Priorities`.
- **Found and fixed live drift inside the register that owns status.** `Delivery Register` row 7 read *"BLOCKED — unblocking 21 Sep"* on 24 September. The 23 September pass had corrected the identical stale phrase in row 3 and missed row 7 one line below it. **Corrected against Aly's own 23 Sep confirmation that the logins are unused.**
- **Named the one step-5 fix worth doing alone:** `Alex Foods` and `Foundation Roadmap` each carry a complete deliverable-by-deliverable status list, so eight statuses live in three places against the register built to own them. That triplication is what produced the row 7 miss, and it will keep producing them.

### What's Still In Progress
- **The merge to `main` needs Aly.** One command from Working Copy, or the sandbox permission relaxed.
- **Step 5 itself stays gated** behind the Foundation shipping, per the plan's own cost argument. Measuring it does not change that.
- **`vault-check` still red on the same two POLEKA findings.** Untouched — `book.py` is source code and the exact edit has not been approved.

### Decisions Made
- **Measuring a deferred job is not doing it.** Step 5 is gated and stays gated; what changed is that it is now a ranked list that can be taken in pieces rather than a week-shaped blob nobody starts.
- **The soft number got labelled as soft.** The 93 prose-status matches are published as a search list with the false-positive rate stated, rather than as a defect count that would look authoritative and be wrong.

### Notes Touched
- `Vault Rebuild Plan` — §8 added, step 5 measured and ranked
- `Delivery Register` — row 7 corrected
- `Active Priorities` — the step-5 item repointed at the measurement
- `2026-09-24` — this session

### Profile Updates
- None.

## Session 4 — 1:05 AM: Is the vault actually restructured? Scored against its own tests

### What Got Done
- **Aly asked whether the work so far means the vault is restructured. It does not, and the plan was built to settle that without an argument** — `Vault Rebuild Plan` §6 states five measurable tests. Scored all five against the vault and wrote the measured column into the plan beside the 20 September one.
- **The result: one row of five moved in four days, and it moved to 4 rather than to 1.**
  - Facts with an enforced single owner: **9**. `check.py` does enforce rule 14, but only for the nine patterns in `owners.tsv` — **the vector-logo status, the plan's own headline example of the six-note problem, has no owner row at all.** Every other fact in the vault runs on discipline.
  - Notes whose body describes a superseded model: **26 notes, 54 inline corrections**, against a target of zero.
  - Places to read the state of 8 deliverables: **4** — `Delivery Register`, `Alex Foods`, `Foundation Roadmap` and `Brand Book Spec` — against a target of one. Was 8.
  - Live versions of the vault: **4** again. `main` plus three branches carrying work it does not have.
  - Drift items found by a full read: **4 found today**, all fixed today.
- **Separated the two things the word "restructured" was covering.** The skeleton — folders, indexes, registers, the hub, the conventions in the boot config — was rebuilt on 20 September, is steps 0 to 4 and 7, and **has held without regression.** The contents have not been rewritten to it, which is step 5 and has not started. **Saying "the rebuild is done" is true of one and false of the other.**

### What's Still In Progress
- **Everything step 5 covers**, still gated behind the Foundation shipping.
- **Four live refs.** Three branches hold work `main` does not, and the sandbox will not let this session write to `main`.

### Decisions Made
- **The answer to "is it restructured" is published as a table, not a claim.** The plan made the tests measurable on purpose; scoring them is cheaper than arguing about it, and it means the next session that wonders can read a number instead of forming an impression.

### Notes Touched
- `Vault Rebuild Plan` — §6 scored with a measured 24 September column
- `2026-09-24` — this session

### Profile Updates
- None.

## Session 5 — 1:40 AM: The one-hour slice — eight statuses, four notes, now one

### What Got Done
- **Aly approved the slice and it is cut.** Read `Delivery Register`, `Alex Foods` and `Foundation Roadmap` end to end first, so nothing was deleted that the register did not already carry.
- **`Alex Foods` held THREE status blocks, not one** — Stage 1, Stage 2 and the client-materials checklist, all with `[x]`/`[~]`/`[ ]` marks and dates. All three are now definitions: **what each deliverable and material IS**, with state a link to the register.
- **It also carried two entries for the same material that openly contradicted each other** — the 18 Sep "no source files exist, logos closed by cut-out" finding sitting directly under the 19 Sep "real vector master arrived" one, the second introduced as *"original entry below, kept because the reasoning still holds."* **That is the exact pattern rule 15 exists to stop**, and it was in the client's own master note.
- **`Foundation Roadmap` was the fourth copy.** Every build-order item stamped its own verdict — "BUILT 18 Sep", "DRAFTED 18 Sep", "Phase 1 is now closed" — and its client-materials section repeated the register's table with its own tick-marks. **Stripped to what the note is actually for: dependency and sequence.** Its materials section became a table of **what each outstanding item blocks**, which is sequencing rather than status.
- **Both notes gained a `## How we got here`** carrying the lifted history: the twice-closed logo material, the AI-photography decision that removed the biggest gate without the client sending anything, the Design System folded into the book, the recipe rename, and how `Decisions` #35 weakened clause 3's 48-hour lever from a contractual mechanism to a drafting habit.
- **Verified by re-measure, not by assertion.** Per-deliverable status verdicts now appear in **exactly one note in the client folder** — `Delivery Register`. Body-history blockquotes in both rewritten notes: **zero**. `structure.py` clean; `check.py` still the same two pre-existing POLEKA findings, zero added.
- **Caught and fixed a cross-reference this session broke.** The risk section said *"the remaining two sit behind items 1 and 2 above"*, pointing at the numbered materials list that had just been replaced by a table. Repointed at the access items by name.

### What's Still In Progress
- **The harder half of step 5: 54 inline history blockquotes across 24 notes.** Unlike the status triplication, these are not mechanical — each is a judgement about what is still true. **`Brand Voice Guide` is the worst at twelve, and it is client-facing.** Still gated behind the Foundation shipping.
- **`main` is still unreachable from this session** and now five commits behind.

### Decisions Made
- **A note keeps what it is FOR, and delegates the rest.** The client note defines what each deliverable *is*; the roadmap says what each one *depends on*; the register says where each one *got to*. Splitting on that line is what made the cut safe — neither note lost anything it was the right home for.
- **Deleted only what the register already held.** Everything unique — the blocking analysis, the two Package A rules on posting and QR measurement, the logo reasoning — was kept, moved to `How we got here`, or reshaped into a blocking table rather than dropped.

### Notes Touched
- `Alex Foods` — three status blocks rewritten as definitions, `How we got here` added
- `Foundation Roadmap` — build order destatused, materials section reshaped into a blocking table, `How we got here` added
- `Vault Rebuild Plan` — §6 scorecard row met, §8 worklist rows struck through
- `Active Priorities` — step-5 item updated to what is left
- `2026-09-24` — this session

### Profile Updates
- None.
